require 'rails_helper'

describe "As an admin user" do
  describe "I see a 'Users' link in the nav bar" do
    it " that is only visible to an admin" do

    end
    describe "When I click on the 'Users' link" do
      it "my current URL route is '/admin/users', which only " do

      end
    end
  end
end
describe "As a regular User" do
  it "I don't see a 'Users' link in the nav bar" do

  end
end

describe "As a merchant User" do
  it "I don't see a 'Users' link in the nav bar" do

  end
end

describe "As a visitor" do

end
